<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.staff.staffAttendance.mapper.AdminReqLeaveMapper">
    <resultMap type="adminReqLeaveDto" id="adminReqLeaveResult">
        <id property="adminReqLeaveSeq" column="adminReqLeaveSeq"/>
        <result property="typeLeave" column="typeLeave"/>
        <result property="status" column="status"/>
        <result property="startDate" column="startDate"/>
        <result property="endDate" column="endDate"/>
        <result property="reason" column="reason"/>
        <result property="username" column="username"/>
        <result property="enabled" column="enabled"/>
    </resultMap>

    <!--    <select id="findAll" resultType="branchDto" parameterType="branchDto">-->
    <!--        SELECT * FROM branches;-->
    <!--    </select>-->

    <!--    <insert id="create" parameterType="branchDto">-->
    <!--        INSERT INTO branches (name, address, description, status) VALUES (#{name}, #{address},#{description}, TRUE);-->
    <!--    </insert>-->


</mapper>